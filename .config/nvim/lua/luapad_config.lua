require 'luapad'.config{
  count_limit = 150000,
  error_indicator = false,
  eval_on_move = true,
  -- context = {
  --   the_answer = 42,
  --   shout = function(str) return(string.upper(str) .. '!') end
  -- }
}
